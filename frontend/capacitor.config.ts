import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'outOfSub.ayoub.com',
  appName: 'out-of-sub',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
